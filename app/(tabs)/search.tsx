// app/search.tsx
// Search Destination screen (modal)

import React from 'react';
import { SearchDestinationScreen } from '../../src/screens/search/SearchDestinationScreen';

export default function Search() {
  return <SearchDestinationScreen />;
}